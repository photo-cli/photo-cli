repos:
  - repo: local
    hooks:
      - id: sync-agent-files
        name: Sync AGENTS.md to agent-specific locations
        language: script
        entry: scripts/sync-agent-files.sh
        files: ^AGENTS\.md$
        pass_filenames: false
